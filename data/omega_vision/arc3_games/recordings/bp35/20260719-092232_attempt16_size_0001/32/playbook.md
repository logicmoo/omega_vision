# Working model

- [Checked, levels 1-9] Logical centers are 6 pixels apart. Cyan 10 is open; patterned 5/3 is terrain. ACTION3 moves west, ACTION4 east, and movement/clicks are followed by gravity settlement. Upward travel normally anchors the avatar at screen y39; downward travel at y29.
- [Checked] ACTION6 is remote within the visible board. Rounded green 14 is removed. Rounded gray 8 is consumed and reverses gravity. Red 12 toggles between solid rounded body and traversable sparse plus. An opened red plus still suppresses white propagation (level-8 step 357).
- [Checked] Rounded 21-pixel white bodies are propagators: clicking removes one and creates bodies in orthogonally adjacent open logical cells; terrain, hazards, objects, existing bodies, and avatar suppress children. Alternating `click blocker; move into its cell` clears a row. Small 12-pixel white sprites are immutable lethal hazards.
- [Checked] ACTION7 undoes the preceding substantive board state but advances the border counter. Need 9 levels; levels 1-8 complete. Avoid RESET: step 512 GAME_OVER already reset at 513, and another reset on a fresh attempt risks the whole game.
- [Checked] `scratch/level9_sim.py` holds the stitched logical level-9 map/model. Upper map came from all death frames at step 512; lower exit/floor is checked from step 467. Raw log is authoritative.

# Working memory

- Level 9 final, current settled step 530, 8/9 complete, upward gravity, avatar world `(27,-45)` / screen `(27,39)`, camera `-84`, fresh-attempt border count 17. Actual board matches `level9_sim.run(OPT_TO_NEG45)`.
- [Checked] Fatal step 512 opened x57 red at world -81 with no overhead support; avatar rose offscreen. Safe crossing is x27 because green `(27,-93)` catches the ascent. Never open x57 while beneath it under upward gravity.
- [Simulated from checked map/mechanics] From current, the known east transfer plus x57 climb takes 21 actions; west propagated clear/move chain plus opening x27 gate takes 11, reaching `(27,-87)` at total 49 before optional gray optimization.
- [Simulated, not yet live] Upper finish from `(27,-87)`: click upper white at world `(45,-123),(39,-123),(33,-123)`; remove green `(27,-93)` to jump under the new `(27,-123)` support; click that support; clear/move west through white x21 and x15, remove/move through green x9, then move to x3. This reaches `(3,-129)` in 12 actions. Click a top-row gray at world y-165 to fall down x3; after all x3 grays are gone, floor y75 catches at `(3,69)`, then E,E reaches exit `(15,69)`.
- [Optimization derived in simulator] Six x3 shaft grays otherwise force reversals. Route necessarily consumes `(-57,-21)`. Before transfer, remove the other four in two down/up pairs: world `-81,+27`, then `-45,+39`; each pair returns to the same `(27,-45)` state. The +39 body center is just offscreen after the down flip but its top pixels are visibly color 8 at screen y61-62; clicking a visible pixel should activate it, but this last point is a deliberate one-action probe, not yet checked live.
- Next: execute the first gray pair and second down flip, then probe `(3,61)` on the clipped +39 gray with avatar-return expectation. If confirmed, continue the fully simulated transfer in a long batch. Estimated optimized completion is about 68 actions, so the 64-cell border may fill before the final few moves; no logged attempt has tested whether full means immediate failure.