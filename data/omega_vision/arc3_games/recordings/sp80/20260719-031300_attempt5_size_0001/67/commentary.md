# Agent commentary (invocation 21)

Step 109’s `[DIFF]` contains only the timer change, so A remained at `(L3,Y10)`. This blocked move reveals the intended geometry: A cannot occupy `(L2,Y10)` because guides must remain separated from cup walls. From the current reachable position, its left branch reaches `(2,11)`, directly outside the sideways cup’s mouth; the likely missing mechanic is that a cup captures flow from this exterior mouth cell. The other branch and the second source route exactly to the three top cups.
