# Agent commentary (invocation 25)

Step 121’s `[DIFF]` is empty, so the L remains at anchor `(3,8)`. The failed left move is explained by cup-specific clearance applied to the L’s rectangular footprint, including its empty corner. This makes `(3,8)` the closest legal placement: when restored to color15 during flow, its cell `(3,9)` may project the same one-cell exclusion onto `(2,10)`, blocking the side branch’s upward escape and forcing it into the cup.
