# Agent commentary (invocation 29)

Step 125’s `[DIFF]` shows direct selection works: C changed `9→8` and the entire L changed `15→9`. Repeating the click on this already-selected nonrectangular entity is now the clean test for a rotation control.
