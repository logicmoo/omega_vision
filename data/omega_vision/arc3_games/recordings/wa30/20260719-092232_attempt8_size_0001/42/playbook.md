# Working model

- **Checked:** The board is a 16×16 logical lattice of 4×4-pixel tiles. `ACTION1/2/3/4` are up/down/left/right; `ACTION5` grabs or releases.
- **Checked:** The player's color-0 edge indicates facing. Unladen directional input faces that way and translates if clear; collision turns in place. A faced adjacent cargo highlights `4→3`; `ACTION5` changes it to held ring 0. Carrying rigidly translates player+cargo without rotating their offset, and the player's zero edge remains toward the cargo regardless of movement direction. Release gives ring 3, later restoring to 4.
- **Checked:** Cargo, player, and agents are solid. Held cargo can overlay color-2 terrain/targets; full color-5 barriers are solid. True targets are color-2 interiors enclosed by color 9; unframed/patterned color 2 is terrain or an alternate-agent destination. Completion occurs immediately when every cargo is released in a framed target; surplus target cells may remain.
- **Checked (levels 2–5):** Color-12 helpers take one autonomous action after each player action when a complete route exists, grab free cargo (`4→5`), carry it rigidly to a feasible framed target, and use a separate release turn (`5→4`). They recompute from post-player occupancy, ignore deposited cargo, and wait without a complete route.
- **Checked (level 6):** Color 15 is an adversarial/reverse helper: it autonomously took cargo from the framed target to an unframed color-2 pad, using ring 5 while carrying. Facing it by attempted movement changes solid 15 to border 11/core 15; `ACTION5` then removes it permanently to underlying floor. Whether this selection also freezes an otherwise mobile color-15 agent is not yet checked.
- **Checked:** The bottom edge is a deadline bar. Level 5 allowed roughly 128 actions; level 6 also had ample time.

# Working memory

- Level 7/9 (6 completed), fresh at step 356. Playable arena is the five-row strip `y=5..9`, enclosed by full color-5 walls at `y=4,10`; outside is color 4. Player `(5,8)` faces up. Cargoes are `(8,6)` and `(7,8)`. Color-15 agent is `(10,8)`. True target is the framed two-cell column `(3,7..8)`; its unframed destination pad is `(12,7..8)`.
- **Assumed, cheap next test:** color 15 should choose nearest cargo `(7,8)`, moving left `10→9→8` on the first two actions and grabbing it on the third. Player will move down to row 9 and begin following. Do not commit the interception until this route is confirmed.
- If confirmed, likely interception is to follow east one row below the carried cargo, become adjacent below the agent after it releases near `(13,8)`, face it, then delete it. **Unchecked:** facing may freeze it before its autonomous phase; test with an expectation rather than assuming.
- After neutralization, deliver both cargoes west to target cells `(3,7)` and `(3,8)` with suitable grab offsets.