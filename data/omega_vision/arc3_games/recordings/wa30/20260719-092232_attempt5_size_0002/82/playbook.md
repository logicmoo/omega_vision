# Working model

- **Checked:** The board is a 16×16 logical lattice of 4×4-pixel tiles. `ACTION1/2/3/4` are up/down/left/right; `ACTION5` grabs or releases.
- **Checked:** The player's color-0 edge indicates facing. Unladen directional input faces that way and translates if clear; against cargo it turns in place. Adjacent faced cargo highlights ring `4→3` on that side.
- **Checked:** Grabbing changes cargo outer ring to 0. While carrying, directional movement rigidly translates player+cargo without rotating their offset. Release gives ring 3; moving/facing away restores ring 4. Cargo may be grabbed from any side.
- **Checked:** Cargo and agents are solid. Held cargo can overlay targets and the repeating patterned color-2 wall tile; underlying tiles restore when vacated. Full color-5 tiles are solid compartment barriers. Releasing every cargo into a target slot completes a level; surplus targets may remain.
- **Checked (levels 2–4):** A color-12 helper normally takes one autonomous action per player action, pathfinds around solids, grabs free cargo (`4→5`), rigidly carries it to a nearest open target, and spends a separate release turn (`5→4`). It recognizes player-released cargo as deposited and ignores it. Path choices are obstacle-aware.
- **Checked (level 4):** With multiple helpers, grabs skip helpers later in upper-to-lower processing order; releases do not appear to. Player/helper are mutually solid, and blocked/rerouted turns still consume deadline.
- **Checked (step 238):** When player and helper were simultaneously adjacent to the same cargo, player `ACTION5` claimed it first (player cargo ring 0; helper moved on), so the player update precedes helper job selection.
- **Checked:** Bottom row is a hard deadline bar consuming right-to-left `7→4` pixels, about one pixel per two player actions on level 5. Avoid delays; this level appears designed for roughly a 96-action solution.

# Working memory

- Level 5 of 9 began after step 203. Full-color-5 barrier has its only passage in rows `y=7..8`. Targets are `x=2..3,y=6..9`; deposits currently `(3,7)` (helper) and `(3,9)` (player). Free top cargo are `(12,1),(14,2)`, free bottom cargo `(15,13)`. One helper.
- **Current after step 238:** player `(12,12)` holds cargo `(13,12)` east (ring 0); helper `(14,11)`. The simultaneous race for original `(13,12)` succeeded. Timer is 18/64 pixels after 35 level actions.
- **Computed intended allocation (strong, but helper's immediate side choice still predicted):** helper should approach `(15,13)` from west via `D,D,grab`, carry it `U×5,L×12`, and deposit `(3,8)` on turn +21. Player should put held cargo at `(2,7)`, then deliberately pass the nearer `(12,1)` and take `(14,2)`; helper takes `(12,1)`. This produces collision-free parallel returns and predicted completion at +59 (about 94 level actions, likely only ~2 turns under intended budget).
- **Current phase, +17 actions:** player path `UUUULLLLLLLLLLLUG` (4 up, 11 left, up, release) reaches player `(1,7)`, deposit `(2,7)`. Predicted helper then is `(5,8)` holding cargo `(6,8)`. Player/helper travel west on row 8 with a four-tile gap; final up avoids conflict.
- **Next phases if confirmed:** from player `(1,7)`, take top cargo `(14,2)` via `URRRDRRRRRRUUUUURRRG` (19 moves then grab; player `(13,2)`). Meanwhile helper finishes `(3,8)` at +21, walks to `(11,1)`, and grabs `(12,1)` at overall +42. Player carries `(14,2)` to `(2,6)` via `DDDDDLLLLLLLLLULLLG` (18 moves, release at overall +56). Helper carries `(12,1)` to `(3,6)` via `DDDDDDLLLLLLLULLG`, releasing at overall +59 and completing level. Retrodicted trajectories do not collide.