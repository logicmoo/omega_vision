# Working model

- **[Checked]** The world is a 3×3-block lattice. A length widget's left hollow lobe retracts and its right hollow lobe extends that actuator by one block, translating its predefined downstream subtree. Color-15 walls and occupied actuator blocks reject a placement; hollow color-13 target crosses are transparent.
- **[Checked]** The main level-7 chain is color11→color14→color9→color12, rooted at block `(16,4)`. State `(n0..n3; d0..d3)` has arm `i` occupying `Qi+k*di, k=0..ni`, and `Q(i+1)=Qi+(ni+1)di`; the terminal head is the last color12 block's center. Rotating actuator `i` rotates that actuator and every downstream direction one counterclockwise screen quarter-turn (`S→E→N→W→S`). Color12 has no rotation widget, so `d3=d2-1` remains invariant.
- **[Checked for all relevant recorded level-7 moves]** A candidate must avoid walls, auxiliaries, and visible self-overlap; each nonempty arm must retain a visible block. Top clipping is allowed, horizontal clipping is not. This simulator exactly retrodicts all accepted/rejected main-chain actions at steps 167–209. In particular step 202's retraction would put the terminal block over color11, and step 209's rejected extension would put a nonterminal color12 block over the fixed color11 base.
- **[Checked]** Direction-widget colored cells all invoke the same one-way turn: level 6's top and bottom clicks both advanced the same cycle. No reverse control is known; the level-7 right-lobe click at step 203 did not perform the geometrically valid reverse turn.
- **[Checked]** Both targets must be occupied simultaneously. Auxiliary color10 is west-facing from block `(11,2)`, currently `n=2`, head `(28,7)`; two extensions at `(60,51)` restore its head to target `(22,7)`. Color8 is north-facing and clear.
- **[Strong computed]** The lower target `(25,16)` has exactly one valid main-chain configuration for `n<=30`: `n=(8,13,7,6)`, dirs `(S,W,N,E)`, with joints at pixel top-lefts `(48,12),(48,39),(6,39),(6,15)`.
- **[Key unresolved hypothesis]** Restricting every `n>=0` makes the full-collision state graph finite (35,006 states through `n<=20`) and disconnects the unique goal, so that restriction must be wrong or another control exists. Allowing `n=-1` as a zero-block/fully retracted actuator yields a clean one-way-rotation, full-collision 40-action route. No recorded action has yet tried retracting an actuator from observed `n=0`.

# Working memory

- **Level 7/8; completed=6; latest step 209.** Correct current main state is `n=(0,1,1,3)`, dirs `(W,N,E,S)`, roots `Q=((16,4),(15,4),(15,2),(17,2))`, terminal head pixel `(52,16)`. The predecessor's `n0=1` was stale: accepted step 205 retracted color11 from 1 to 0. Steps 206–208 then made `n2=1,n1=1`; step 209's attempted `n0:0→1` was rejected by color12/base overlap.
- **Next deliberate probe:** click color11 retract `(4,51)` at observed `n0=0`. If zero-block `n0=-1` is legal, the color11 world block disappears and the whole downstream subtree shifts east one block, putting the head at pixel `(55,16)`. If rejected, `n=0` really is minimum and the next investigation must target a hidden reverse-turn input rather than relax collision.
- **Ruled out:** treating step 209 as mere terminal-head collision; current-state off-by-one model; unrestricted body crossing; over-right-edge placement; dynamic capture/pushing of color8; remote wall effects.
- Timer/HUD remains far from expiry (only about 13 edge ticks filled by step 209).
