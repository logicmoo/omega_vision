# Working model

- **[Checked, levels 1–6]** `ACTION6` clicks color-matched actuator controls. A length panel's left/right glyph retracts/extends by 3 cells. The selected actuator changes length and translates its recursively downstream subtree by 3 along its direction.
- **[Checked, levels 1–6]** A lone magenta/color-13 cell is an active head; a hollow four-arm cross is a target. Docking gives a solid plus, and every head must be docked simultaneously. A previously docked head may be moved and restored.
- **[Checked]** Color-15 is fixed collision geometry; color-1 blocks can be pushed downstream. Any collision rejects the whole color action. UI panels and target arms are collision-transparent overlays.
- **[Checked, level 6]** A direction-plus arm sets the selected actuator to that absolute compass direction and rigidly rotates every downstream actuator by the same turn. Downstream relative orientations are preserved; length changes do not rotate them.
- **[Checked kinematics, levels 6–7]** For a width-3 actuator, use top-left joint coordinate `Q`, direction `d`, and `l=2+3n`. `Qchild=Q+(l+1)d`; occupied cells are the corresponding `(l+1)×3` cap/body rectangle; an active head is `Q+(1,1)+(l-2)d`.
- **[Checked only for partial clipping]** Direction changes can put part of an actuator off-board while hidden geometry remains in state (level 6 step 143). **[Newly checked, step 180]** Final-state collision validity alone is insufficient for a length move: extending color11 north would have translated its color14 child from visible `y0..2` to wholly above the board; the game showed the attempted frame and rolled back with settled `[DIFF] none`. Best current hypothesis is that translational length moves cannot push a downstream actuator completely off-board; direction changes may still rotate a subtree wholly off-board, but that is not yet checked.

# Working memory

- **Level 7/8; levels_completed=6.** Color10 remains docked at target `(22,7)`. The color12 head must dock at hollow target `(25,16)`. Leave the separate color10 and color8 actuators untouched.
- Main chain order is color11→color14→color9→color12. State after step 180 (the rejected move did not change it): coefficients `(A,B,C,D)=((l11+1)/3,(l14+1)/3,(l9+1)/3,(l12-2)/3)=(4,4,6,4)` and directions `(N,E,S,W)`. Joints are `Q11=(48,12), Q14=(48,0), Q9=(60,0), Q12=(60,18)`; active head is `(49,19)`.
- Current occupied main rectangles are: color11 `x48..50,y3..14`; color14 `x48..59,y0..2`; color9 `x60..62,y0..17`; color12 `x48..62,y18..20`. These exactly match the settled board.
- The first 13 actions of the prior route all matched predictions. Ruled out: directly extending north-pointing color11 once more from this state; it rolls back because its horizontal color14 child would leave above the board.
- Controls needed: color11 extend `(10,51)`, direction right/down/up `(18,51)/(17,52)/(17,50)`; color14 direction up/left `(17,57)/(16,58)`; color9 direction down `(39,52)`.
- **[Computed under the boundary-translation hypothesis]** A 6-action continuation reaches the same collision-free goal while performing the needed color11 extension in-bounds: (1) set color14 north; (2) set outer color11 south; (3) extend color11 south once; (4) set color11 north; (5) set color14 west; (6) set color9 south and dock. This gives states/active positions: `(4,4,6,4; N,N,E,S)` active mathematically `(67,1)`; then `(S,S,W,N)` active `(31,25)`; extend to A5 active `(31,28)`; rotate outer north active `(67,-2)`; color14 west active `(49,-20)`; color9 south active `(25,16)`.
- **Next is one deliberate probe only:** click color14 up `(17,57)`. Final-state geometry predicts acceptance, with color14 visible vertically at `x48..50,y0..2`, while color9/color12 rotate wholly off-board; expect `(49,2)=3`, `(60,0)=5`, and old active cell `(49,19)=5`. Acceptance checks the unverified ability of a direction change to preserve a wholly hidden subtree before committing the remaining five actions.
