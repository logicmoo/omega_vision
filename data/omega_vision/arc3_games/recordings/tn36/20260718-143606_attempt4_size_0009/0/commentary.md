# Agent commentary (invocation 15)

Level 4 introduces scale: the upper yellow object is exactly a 2× enlargement of the normal down-facing plug, while the lower socket accepts the normal-sized version. The six command slots match the likely route `SHRINK, LEFT, DOWN, DOWN, DOWN, DOWN` through the wall gap. The single-dot selector should demonstrate the required shrink operation and expose its code.
