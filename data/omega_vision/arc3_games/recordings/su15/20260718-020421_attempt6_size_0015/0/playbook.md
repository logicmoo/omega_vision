# Working model

- **Checked (levels 1-4):** ACTION6 clicks a grid coordinate. A playfield click within range relocates the nearest compact field piece and makes the click its logical anchor; anchor displacement up to 4 in each axis works (including diagonal), while farther clicks are no-ops. ACTION7 undoes prior actions, including hazard damage and pursuit, without restoring deadline cells spent before the undo chain.
- **Checked:** Rendered footprints by stage are: color10 size1 at anchor; color6 size2 bbox `[x-1..x,y-1..y]`; color15 size3 bbox `[x-1..x+1,y-1..y+1]`; color11 size4 bbox `[x-2..x+1,y-2..y+1]`.
- **Checked:** The HUD ladder is 1x1 color10 -> 2x2 color6 -> 3x3 color15 -> 4x4 color11. Moving one piece into merge range of another same-stage piece consumes both and creates the next stage at the mover's clicked anchor. Coordinatewise anchor thresholds are size1 pair -> size2 at 5, size2 pair -> size3 at 6, size3 pair -> size4 at 7, including diagonal cases.
- **Checked:** Large centered HUD shapes are required outputs; leaf-equivalent mass has matched them. A single-output level completes when the final piece's logical anchor enters its color9 blob. Level 3 required both outputs simultaneously in its two blobs (successful assignment size4 left, size3 right).
- **Checked:** Rows 0-9 are HUD; row 63 is a deadline bar and not gameplay. It drains about 1-2 cells per click; invalid clicks also spend it.
- **Checked (level 4):** A rounded color7 hunter moves independently up to 4 per axis each action toward the nearest compact field piece's current/new anchor. It advances through edge adjacency and attacks on contact: a size2 was downgraded and its remaining single flung away; contact can also corrupt a color9 blob. Undo hazard contact immediately unless the victim is known expendable.
- **Checked (level 4):** A hunter is directly movable by clicking within 4 cells per axis. It launches to `center + 2*(click-center)`, clipped so its footprint stays in bounds: `(54,21)` via `(58,17)` settled `(61,13)`, and `(41,32)` via `(45,28)` settled `(49,24)`. This is the intended repulsion control.

# Working memory

- Level 5/9 (`levels_completed=4`), fresh after step84; timer 64/64. HUD requires one size4; color9 target is centered `(32,15)`, bbox `(28,11)-(36,19)`.
- Useful upper mass is already four size2 anchors: A=`(7,26)`, B=`(15,29)`, C=`(43,27)`, D=`(54,27)`. Four extra singles at `(44,53),(14,54),(58,59),(3,60)` appear to be sacrificial decoys. Two hunters start centered `(6,39)` and `(48,39)`.
- **Retrodicted plan:** repel left hunter with `(2,43)` to clipped center `(2,47)` while right pursues to `(44,35)`; repel right via `(48,39)` to `(52,43)` while left pursues its bottom-left decoy to `(3,51)`. Then both should chase lower singles rather than the required upper pieces.
- Productive minimum is 10 clicks: merge A+B at `(13,25)`; D via `(50,23)->(46,23)` merges C; move right size3 `(42,19)->(38,19)`; move left size3 `(17,21)->(21,19)->(25,19)->(29,19)->(32,19)`. The last click merges at a color9 anchor, completing immediately. Initial simulation says the four expendable singles occupy the hunters long enough; confirm the two-hunter/decoy behavior in short batches before committing through contact.
