# Agent commentary (invocation 46)

The isolated probe confirmed that color8 relocates to the clicked anchor from about 12 cells away, while the hunter pursues normally. Moving it to `(34,19)` creates a controlled right-side approach; the following far click should produce the first attack and reveal the exact color8-to-color12 knockback.
