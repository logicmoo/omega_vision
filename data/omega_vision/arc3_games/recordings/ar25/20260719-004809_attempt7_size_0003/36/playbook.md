# Working model

- **Checked through levels 1-6:** Color-5 patterned sources move; color-4 copies are reflections across color-10 mirrors. Logical cells are 3x3 pixels. Cover every required color-11 cell with a source/image to complete a level; extra reflected cells on background are allowed.
- **Selection/controls (checked):** Black color-0 centers mark the selected entity. ACTION1=up, ACTION2=down, ACTION3=left, ACTION4=right, ACTION5=next selection, ACTION7=undo; ACTION6 remains untested. Selected source/mirror centers are black on background, while centers over goals continue to show underlying color11.
- **Reflection dynamics (checked):** A source move `(dx,dy)` induces the reflected displacement in all its copies. Moving a mirror one logical cell along its normal leaves sources fixed and moves copies involving that mirror by two cells. Perpendicular mirrors give source, horizontal-only, vertical-only, and double-reflected copies.
- **Multiple sources (checked on L6):** Shapes can be diagonally/8-connected despite splitting under 4-connectivity; all diagonal pieces selected and moved together. ACTION5 cycles separate source entities after mirrors. Do not infer source order from scan order: on L6 the right/lower 5-cell source came before the other source.
- **Targets (checked):** Yellow patterns may be sparse marker subsets, not complete silhouettes. Solve by requiring `G ⊆ union(source reflection orbits)`; extra occupied cells are legal.
- **HUD:** x=63 spends one cell/action and resets each level; y=63 is static. Treat it as a 64-action budget. Undo restores geometry but not HUD.

# Working memory

- **Level 7/8, fresh after step172 (6 completed).** Horizontal mirror at macro `y=5` is selected; vertical mirror at `x=3` is unselected. Two 4-connected sources: A has 14 cells in bbox macro x5..9,y16..19; B has 9 cells in x17..19,y13..19.
- Python exhaustive search over all in-board integer mirror axes and all translations proved no solution with fewer than 34 geometry moves. Unique known optimum: mirrors to `x=12,y=7` (vertical right9, horizontal down2); A translation `(+3,-6)` (cost9); B translation `(-2,-12)` (cost14). Their orbits cover all 42 goals (A covers 28, B 14). With three ACTION5 selections the expected level cost is 37, safely under HUD.
- Likely selection order, based on L6, is horizontal -> vertical -> right-hand B -> A, but B-first is not yet checked on L7. Gate the second ACTION5 with a B center expectation.
- Immediate sequence: horizontal down2; ACTION5; vertical right9; ACTION5. If B cell macro `(19,13)` (physical center `(58,40)`) becomes black, move B left2/up12; ACTION5 should then select A; move A right3/up6 to finish. Final B source cells are `(17,1),(17,2),(16,3),(17,3),(16,4),(15,5),(16,5),(15,6),(15,7)`; final A source is bbox x8..12,y10..13.