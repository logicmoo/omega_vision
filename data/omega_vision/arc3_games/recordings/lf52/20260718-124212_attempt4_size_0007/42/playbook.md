# Working model

- **[Checked through level 4]** The game is orthogonal peg solitaire on a 6-pixel lattice. Color-14 crosses are ordinary pegs. A jump moves a peg two orthogonal lattice steps into an empty playable cell; jumping an ordinary peg removes it. Color-15/7 squares are permanent hurdles: they count as jump-over occupancy but remain, so crossing one relocates a peg without reducing the ordinary-peg count.
- **[Checked]** Use `ACTION6` on source then destination. Selection outlines the source in color 3 and puts color-2 rings on legal destinations. `ACTION7` undoes the last jump and restores its prior selection state.
- **[Checked through level 4]** Color-11/12 shuttles move on color-5 rails under `ACTION1` up, `ACTION2` down, `ACTION3` left, `ACTION4` right. One input moves every independently legal shuttle; blocked ones stay. A shuttle is a playable source/destination and can carry an ordinary peg. Level 4 confirmed that a peg can hurdle-jump directly from one shuttle into another.
- **[Checked levels 1–4]** A level completes with exactly one ordinary peg, including one carried by a shuttle. Levels may consist of staged offscreen boards; reduce each component and relay its survivor.
- **[Checked]** Camera following/scripted pans can make screen positions differ from world positions. Large full-board diffs after valid actions have been camera motion. Row 0 is a 64-action deadline bar. Prefer undo to reset; never double-RESET.

# Working memory

- Level 4 completed at step 157 with the predicted final lower-row jump. Level 5 is now `4/10`, timer 0/64, camera reset, all controls plus undo available.
- Level 5 introduces an apparent **hurdle-carrying shuttle**: empty shuttle at `(19.5,7.5)` and a second shuttle at `(43.5,7.5)` visibly containing a color-15/7 hurdle. Whether the hurdle rides with the shuttle and remains valid jump-over occupancy is the new mechanic to confirm.
- Left board is a full 3x3 with centers x`13.5,19.5,25.5`, y`19.5,25.5,31.5`; its middle row is ordinary peg `(13.5,25.5)`, fixed hurdle `(19.5,25.5)`, empty `(25.5,25.5)`. Right two-cell board at y25.5 has empty `(37.5,25.5)` and peg `(43.5,25.5)`. Total visible ordinary pegs: 2.
- Rail graph: top horizontal y7.5 has x13.5..49.5; central vertical x31.5 runs y7.5..43.5; bottom horizontal y43.5 connects x19.5..49.5; right vertical x49.5 runs y19.5..43.5; left vertical x19.5 descends from y43.5. Thus the right dock is `(49.5,25.5)` and is reached through the central/bottom route, not directly from top x49.5.
- Intended bridge hypothesis: first move the left peg `13.5→25.5` across its fixed hurdle. Move hurdle shuttle left twice `43.5→31.5` (empty shuttle goes `19.5→13.5` then blocks) and down three times to `(31.5,25.5)`. If mobile hurdle cargo works, selecting the peg at25.5 should mark right-board `(37.5,25.5)` across that shuttle. Test this exact setup now.
- If confirmed, jump to x37.5. The empty shuttle must later route via central x31.5 and bottom/right rails to dock `(49.5,25.5)`, after moving the hurdle shuttle out of its way; then `37.5→49.5` over the right peg should finish. Optimize the simultaneous cart schedule only after confirming the mobile-hurdle bridge.
