# Working model

- **[Checked through level 4]** The core game is orthogonal peg solitaire on a 6-pixel lattice. Color-14 crosses are ordinary pegs. A jump moves a peg two orthogonal lattice steps into an empty playable cell; jumping an ordinary peg removes it. Level 4 adds permanent color-15/7 hurdles: they count as jump-over occupancy but remain, so crossing one relocates a peg without reducing the ordinary-peg count.
- **[Checked]** Use `ACTION6` on source then destination. A selected source gets a color-3 outline and legal destinations color-2 rings. A peg with no move only shakes. `ACTION7` undoes the last jump and restores its prior selection state.
- **[Checked through level 4]** Color-11/12 shuttles move on color-5 rails under `ACTION1` up, `ACTION2` down, `ACTION3` left, `ACTION4` right. One direction input moves every shuttle for which that direction is legal; blocked shuttles stay put. A docked shuttle is a playable landing/source cell and can carry a peg between boards.
- **[Checked levels 1–3]** A level completes with exactly one ordinary peg, including one carried by a shuttle. Disconnected components must be reduced and joined through shuttles.
- **[Checked levels 3–4]** The camera may follow a moving shuttle or make a scripted pan after a sub-board is reduced. Track world coordinates separately from screen coordinates; a large full-board diff after a valid jump has repeatedly been camera motion, not a failed jump.
- **[Checked]** Row 0 is a 64-action deadline bar advancing once per action. Dead positions with multiple pegs recolor survivors and show a reset icon; prefer undo and never double-RESET.

# Working memory

- Level 4 is at `3/10`, step 132, timer 25/64. The initial hurdle row and the large six-column central board have both been solved as forced chains. The last central jump loaded its sole survivor into the second shuttle and triggered a scripted diagonal camera pan; no expectation failed.
- **[Checked from world continuity]** Current camera offset is `(world-screen)=(+39,+30)`. Shuttles are: first/upper empty at world `(73.5,25.5)` (offscreen); second **loaded** at world `(79.5,49.5)`, screen `(40.5,19.5)`; newly revealed third empty at world `(61.5,73.5)`, screen `(22.5,43.5)`.
- **[Checked]** Remaining visible board pegs are clipped at screen x=0 but correspond to world `(37.5,61.5)` and `(37.5,85.5)`. The pan revealed a lower relay region. Hurdles include world `(43.5,67.5)`, `(55.5,67.5)`, and `(43.5,79.5)`; more structure can be exposed by moving left.
- **[Checked geometry]** The loaded second shuttle can move left along `y=49.5` from world x79.5 toward a vertical rail at x55.5. The third empty shuttle can also move left along `y=73.5`. Therefore left is the necessary next direction, although simultaneous-cart positioning and camera behavior in this new region are not yet settled.
- **[Provisional, do not chain on yet]** The aligned hurdle at world `(55.5,67.5)` suggests a relay: eventually place the loaded second shuttle at `(55.5,61.5)` and the third empty shuttle at `(55.5,73.5)`, then hurdle-jump the payload downward between shuttles. First test one left input. Predicted world movement is -6 for all left-legal shuttles; likely the camera follows the loaded shuttle -6, leaving the two visible shuttle centers at the same screen positions. Re-derive from the result before planning the synchronization sequence.
