# Working model

- **[Checked through level 4 and level-5 relays]** This is orthogonal peg solitaire on a 6-pixel lattice. Color-14 crosses are ordinary pegs. A jump moves a peg two orthogonal lattice steps into an empty playable cell; an ordinary midpoint peg is removed. Color-15/7 squares are permanent hurdles: they enable the jump but remain.
- **[Checked]** `ACTION6` selects source then destination. Selection outlines source in color3 and marks legal destinations with color2. `ACTION7` undoes the last jump. Color-11/12 shuttles move on color-5 rails under `ACTION1/2/3/4 = U/D/L/R`; one input moves every independently legal shuttle, while blocked shuttles stay. A shuttle is a playable source/destination and can carry a peg; a hurdle can ride a shuttle and remains hurdle cargo.
- **[Checked]** A level ends only at one ordinary peg across the full world. Local reductions can pan to offscreen components, so relay the survivor through every revealed stage.
- **[Checked against levels 3 and 5]** Camera motion changes screen but not world coordinates. In horizontal relay sections, when an ordinary-loaded survivor cart moves, the camera can follow it by the same 6 pixels: that cart stays at fixed screen x while scenery and blocked carts shift oppositely. Hurdle-only/empty-cart movement did not cause such following in level-5 stage 1. Large full-board diffs are usually camera pans.
- **[Checked as counter; deadline now doubtful]** Row0 increments once per action and has 64 cells. No timeout has occurred. The checked current rail/cell graph has a 55-action shortest continuation from timer34, so a hard 64-action per-level deadline is incompatible with the visible puzzle unless an unobserved shortcut exists; monitor what happens at64 rather than distorting the solution.
- Prefer undo to reset. Never issue two RESETs in a row.

# Working memory

- Level 5 is `4/10`, timer34 after step191, all controls plus undo available. The prior route worked: survivor cart docked at screen31.5 and jumped `31.5→43.5`; a 33-pixel left camera pan exposed the large next stage.
- **[Checked current board]** Ordinary pegs: `(52.5,7.5)`, survivor `(10.5,25.5)`, `(46.5,31.5)`, `(34.5,55.5)`. Fixed hurdles (cell centers despite visual y-centroid −1): `(34.5,7.5)`, `(28.5,25.5)`, `(34.5,31.5)`, `(28.5,49.5)`. Mobile hurdle cart is `(58.5,37.5)`. The old empty survivor cart is truncated at `(-1.5,25.5)` and is irrelevant.
- **[Checked from rail pixels]** Mobile-hurdle rail is three verticals x`16.5,40.5,58.5` joining horizontals y`13.5,37.5`, with nodes every6. Current hurdle cart must first go `LLLLLLLUU` to `(16.5,25.5)`, bridging survivor `10.5→22.5`.
- **[Python Dijkstra over checked cells/rails; later jumps not yet live-checked on this stage]** Shortest continuation costs 55 actions (31 direction inputs + 12 two-click jumps). Full world-coordinate route, segmented at jumps (`J`): `LLLLLLLUU; J10,25→22,25(cart16); UURRRRDD; J22,25→34,25(fixed28); J34,25→46,25(cart40); UUR; J46,31→46,19(over peg46,25); J46,19→46,7(cart46,13); LLL; J52,7→40,7(over peg46); J40,7→28,7(fixed34); J28,7→28,19(cart28,13); LLDDDDRR; J28,19→28,31(fixed28,25); J28,31→28,43(cart28,37); J28,43→28,55(fixed28,49); J34,55→22,55(over peg28)`.
- Current immediate plan: move hurdle cart seven left and one up. No camera-follow is expected because it carries only a hurdle. Predicted settled cart after this prefix: `(16.5,31.5)`; then one more up and the first bridge jump.