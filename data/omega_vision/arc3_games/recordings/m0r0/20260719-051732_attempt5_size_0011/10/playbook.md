# Working model

- **Confirmed objective (L1–L4):** two cyan pieces move synchronously by one lattice tile; a level completes when both finish an input on the same tile and merge.
- **Confirmed normal controls:** ACTION1=up, ACTION2=down. ACTION3 is mirrored outward (left-origin piece left, right-origin piece right); ACTION4 is mirrored inward (left-origin right, right-origin left). Each piece is blocked independently, enabling desynchronization.
- Gray 5 is floor; solid side-colored tiles are walls (colors vary by level). **Confirmed hazard:** gray/azure 5+8 checkerboard is enterable, but landing either player on it blinks that player and soft-resets both to their level starts while preserving timer use.
- **Confirmed movable-marker mechanic (L3–L4):** maroon 9 markers block players. ACTION6 selects one (marker 9→11, players 10→1); directional keys move only it literally (up/down/left/right). Walls, markers, and players block it. ACTION6 on another marker switches selection; ACTION6 on a player/floor deselects. ACTION5 had no effect on markers.
- Timer is the advancing top/bottom border, roughly one cell per two actions; it is HUD, not gameplay.

# Working memory

- Level 5, 4/6 complete, fresh board. Lattice origin `(2,2)`, tile size 4, 15×15. Players start left `(1,12)`, right `(13,12)`. Structural walls are color 6 on the left and 7 on the right; central divider `x=7` blocks rows 0 and 5–14, while rows 1–4 connect across the top.
- Tile map (`.` gray; `6/7` structural walls; other symbols are solid special colors): `6666666*7777777 / ...#........... / ............... / ............... / ............... / 666EEE6*77###77 / .......*E.....C / .......*7.....7 / .......*....... / 66CCC66*77###77 / .......*....... / ...6...*....... / .P.#...*.....P. / .......*....... / 6666666*7777777`, where `#`=15, `C`=12, `E`=14, `*`=split 6/7 divider.
- **Unconfirmed key/door hypothesis:** singleton non-structural colored tiles are keys and same-colored 3-tile horizontal components are doors. This would make the start key 15 at `(3,12)` open the right-side color-15 gates; later right-side singleton keys 12 `(14,6)` and 14 `(8,6)` would open the corresponding left gates. Singleton structural-color 6 `(3,11)` is probably just a wall. Do not plan beyond a probe until confirmed.
- Immediate deliberate probe: two inward moves. First should put players at `(2,12),(12,12)`. On the second, key hypothesis predicts left enters the singleton 15 tile `(3,12)` as cyan while right reaches `(11,12)`; inspect the full diff afterward to see which color-15 components, if any, change. If left remains at `(2,12)`, the singleton is a wall and the hypothesis is falsified.