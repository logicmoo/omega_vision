# Working model

- **Confirmed objective (L1–L2):** two cyan pieces move synchronously by one lattice tile; the level completes when both finish an input on the same tile and merge.
- **Confirmed normal controls:** ACTION1=up, ACTION2=down. ACTION3 is mirrored outward (left-origin piece left, right-origin piece right); ACTION4 is mirrored inward (left-origin right, right-origin left). Each piece is blocked independently, enabling desynchronization.
- Gray 5 is floor. Solid side-colored regions (15 on left, 8 on right, including split-color central tiles) are walls. **Confirmed L2 hazard:** gray/azure checkerboard is enterable but soft-resets both players while retaining timer use; avoid it.
- **Confirmed movable-marker mechanic (L3):** maroon 9 markers block players. ACTION6 on one selects it: selected marker becomes yellow 11 and players become inactive blue 1. Directional keys then move only that marker literally (ACTION1 up, ACTION3 left, ACTION4 right confirmed; ACTION2 down strongly inferred). Walls, markers, and players block it. ACTION6 on another marker switches selection directly; ACTION6 on a player or empty tile deselects and restores markers 9 / players 10. ACTION5 does nothing here.
- Timer is the advancing top/bottom border, roughly one cell per two actions; it is HUD, not gameplay.

# Working memory

- Level 3, 2/6 complete. Lattice origin `(6,10)`, tile size 4, 12×11 tiles. Walkability (`.` floor, `#` wall): `##.####..... / #...#...###. / #.#.#.###.#. / #.#.#.#..... / #.#...#.#### / #.#####..... / ......####.. / #####.#..#.# / ##....#..... / ##....#..... / ##....#.....`.
- Players have not moved since the probe prefix: left `(4,8)`, right `(9,5)`, currently blue/inactive. Right marker is parked at dead-end `(9,2)` as 9. Left marker is parked **and selected** at dead-end `(2,0)` as 11. Central marker remains `(6,1)` as 9. Timer has about 11 edge cells spent.
- Direct marker switching and the parking moves succeeded. Next: click central marker `(31,15)` and park it permanently at dead-end `(0,6)` using literal marker moves `L DDD LL UUU LL DDDDD L`, then click player `(23,43)` to deselect. This route is clear; first down is the only marker direction not yet directly logged, so guard it with an expectation.
- A combined BFS under the confirmed mechanics found a 43-action shortest continuation from the current state with the two side markers fixed: central select + the 17 moves above + deselect, then normal-player inputs `IIUU OOOO UUUUU IIII D II DDD I` (`I`=ACTION4 inward, `O`=ACTION3 outward). Predicted final merge is tile `(4,4)`. Retain per-action expectations when executing.
- Ruled out: markers being passable/collectible, pushable by players, removed by ACTION5, unlocked by one click, swapped with a player, or relocated by clicking floor.