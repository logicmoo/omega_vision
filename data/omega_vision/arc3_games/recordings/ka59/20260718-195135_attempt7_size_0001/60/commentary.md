# Agent commentary (invocation 9)

The second launch succeeded: T is now at its required top-left anchor `(5,2)`, V is selected at `(12,2)`, and the pulse phase is 1. The revised three-cell-wave model correctly predicted that V would be safe while beside T.

V now needs to return through the one-cell-wide column-11 corridor and stop at `(9,10)`, where H can later launch it into the isolated left chamber. The two pulse firings along this route occur outside both pulses’ horizontal spans.

## Predicted cells (x, y, old, new)

[[33, 12, 14]]
