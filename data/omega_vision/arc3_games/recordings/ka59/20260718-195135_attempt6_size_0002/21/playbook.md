# Working model

- **Checked through level 5:** ACTION1/2/3/4 are up/down/left/right. A selected green (core0) moves one logical tile = 3 board cells per arrow and restores terrain. Color1/color4 are traversable; color2/color15 block direct movement. ACTION6 switches green cores.
- **Checked through level 5:** Color4 outlines are shape-matched goals; the level completes when all required pieces fill them.
- **Checked through level 5:** Contacting an inactive green or passive object (color11 or a 12/13 block) launches it while the selected pusher stays fixed. Landing is five logical tiles away if clear; otherwise continue forward to first clear aligned landing, or clamp before an edge if nothing is clear beyond five. Color4 is nonblocking; sparse contact stances work.
- **Pulse mechanic, checked level5 and oriented in level6 step171:** color13 fills with color12 one board-cell row/column per action. At full12 it emits a 9-cell/3-logical-tile wave in the fill direction and resets13; caught selected pieces are carried. Direction is object-specific. In level6, both upper blocks first turned12 on their top row, so they fill/emit downward; the bottom block first turned12 on its bottom row, so it fills/emits upward. All three are period6 and synchronized. A pulse-driven landing into color15 is strongly expected to use the same continue-to-clear rule, but has not yet been observed.
- **HUD:** row63 color4 is the level budget.

# Working memory

- Level 6 of 7, levels_completed=5. Current after step171: selected green center(49,49), immediately right of bottom 6x6 block bbox42..47,y45..50. All pulse blocks phase1/6. Passive plus center(10,19), goal center(7,7); green goal center(49,22). Upper-left/right blocks are bbox24..29 and33..38,y15..20. Static partitions: divider x30..32; color15 y30..35 on both sides.
- Proven orientation resolves transport: move the bottom upward-pulse block horizontally to choose which upper room receives the green. Intended efficient order is left room first for the plus, return via the upper-left downward pulse, then move bottom block right and ride it to the green goal room.
- Current computed probe: launch bottom block left twice, positions x42..47 ->27..32 ->12..17. Route green above its left half and synchronize phase6. Exact sequence from current: push-left; walk left5 to center(34,49); push-left; up2,left6 to(16,43); right then left on phases5/6. The final upward pulse should carry green center(16,43) toward nominal34 (blocked by color15) and continue to first clear center(16,28). Upper-left downward pulse is simultaneous but x24..29, disjoint.
- If transport confirms: from center(16,28), use sparse stance(13,22) to launch plus up; edge clamp gives plus center(10,7). Move pusher to(13,10), launch plus left; edge clamp gives goal center(7,7). Then synchronize under upper-left block to pulse green down to bottom, launch bottom block right twice, pulse up at x46, and walk to green goal. Keep phases modulo6 explicit.