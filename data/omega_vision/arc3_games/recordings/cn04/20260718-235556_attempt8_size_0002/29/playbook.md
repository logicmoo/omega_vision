# Working model

- **Checked through levels 1-4:** The board uses 3x3-pixel logical cells. ACTION1/2/3/4 move the active piece one logical cell up/down/left/right. ACTION5 rotates an ordinary active piece 90° clockwise around its unchanged top-left anchor. Pieces can pass through one another; vacated layers are restored.
- **Checked through levels 1-4:** Color8 cells are ports. Only exact overlay of two ports is a valid match (shown as color3 while one participant is active). Edge contact, port-on-body, body/body overlap, and three ports at one coordinate are invalid. Inactive pieces become uniform color4 and hide ports. ACTION6 selects the clicked piece; clicking the active piece can leave none active.
- **Checked on levels 3-4:** Completion used a collision-free arrangement pairing every port. Level 5 follows the same closure objective after its special piece finishes unfolding.
- **Level-5 special mechanic, checked through the complete four-step unfolding:** P begins as a rooted partial tree (color0 root, color11 body). Each ACTION5 consumes the root into body and reveals the next row. The four appended rows are exactly `RA`, `.AAAAR`, `RA`, `.AAAR` (left1, right4, left1, right3). The fourth extension creates no new root: P is then complete with seven ports. Earlier inference of an infinite ruler sequence / six extensions was falsified at this fourth action.
- **Level-5 solution structure, exhaustively checked geometrically:** Completed P has 7 ports and Q/R/S together have 7. Exhaustive rotations/translations found exactly one collision-free relative arrangement pairing all 14 ports: with P at `(px,py)`, Q is `(px+2,py),r3`, R is `(px-2,py+1),r0`, and S is `(px+4,py+4),r3`. Thus the apparent rooted-tree objective was wrong; the root is an unfolding cursor, and every final port is paired as before.
- **Checked on Q:** It rotates normally with ACTION5. R and S are ordinary pieces and are expected to do so too; the level-5 solution leaves R unrotated and rotates S/Q three times.
- **Checked:** The strip `(16..47,0)` is deadline HUD. It advances roughly once per three actions, so the current 40-action remainder is safely within its budget.

# Working memory

- Level 5 of 6, current log step 113. Four guarded ACTION5 actions executed; the fourth expectation failed only because it revealed the terminal row `.AAAR` and removed the root. P is complete and active at logical anchor `(7,4)` / pixel top-left `(23,14)`. Q=`(15,1),r0`, R=`(1,12),r0`, S=`(15,15),r0`.
- Keep P fixed. Choose the minimum-cost global placement at P's current anchor: Q target `(9,4),r3`, R target `(5,5),r0`, S target `(11,8),r3`. This unique closed assembly costs 40 more actions: R 12 (select, up7, right4), S 15 (select, left4, up7, rotate3), Q 13 (select, left6, down3, rotate3). Any ordering is valid; finish with Q so its last rotation should raise LEVELS to 5.
- Immediate next chunk: select R at pixel `(6,39)`, move up7 and right4. Its three final matches with P are logical `(7,5),(7,7),(7,9)`, pixel centers `(24,18),(24,24),(24,30)`, all color3 while R is active.
- Ruled out: rotating P; stopping P before all four extensions; the infinite-ruler/six-extension model; a level-5 objective allowing exposed leaves; every fully paired geometry except the unique relative pose above.