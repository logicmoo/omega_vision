# Working model

- **Checked through levels 1-4:** Boards use a 3x3-pixel logical lattice. ACTION1/2/3/4 translate the active piece one logical cell up/down/left/right. For ordinary pieces ACTION5 rotates 90° clockwise around the unchanged top-left anchor. Only the active piece moves; pieces can pass through one another and hidden layers are restored.
- **Checked through levels 1-4:** Color8 cells are ports. Exact overlay of two ports displays color3 when one piece is active; edge adjacency, port-on-body, body overlaps, and three ports at one coordinate are invalid. Inactive pieces are uniform color4 and hide ports; ACTION6 selects the clicked piece, revealing its body color/ports, and clicking the active piece can leave none active.
- **Checked on levels 3-4:** Their intended closed arrangements paired every port and had no other overlap. Do not carry the closed-cycle objective into level 5: exhaustive placement search proves level 5 cannot pair all ports at P growth states 0 or 2, and (under the strongly inferred growth sequence) cannot at state 4 either.
- **Level-5 special piece, checked for its first two extensions:** P is a rooted tree (color0 root, color11 body). ACTION5 extends rather than rotates: it converts the old root to body, appends one side branch ending in a color8 port, and moves the root down one row. Existing+observed branch lengths by row are right1, left1, right2, left1, right4; this strongly indicates the ruler sequence, predicting next rows left1, right2, left1, right8.
- **Level-5 ordinary piece check:** Q ACTION5 rotated it clockwise exactly as in prior levels. R and S should also rotate normally, though the current candidate uses them unrotated.
- **Strong level-5 objective inference, not yet completed live:** The four objects are tree fragments (rooted P plus Q/R/S). Level 5 should require one collision-free rooted tree: connect all four fragments with exactly three two-port joins, leaving the other ports as leaves. This explains the root marker and why closing every port is impossible.
- **Checked:** The color4 strip `(16..47,0)` is deadline HUD; ignore it in gameplay expectations. It advances only intermittently and permits well over the planned action count.

# Working memory

- Level 5 of 6, current log step 109, fresh after one RESET. P is active at logical anchor `(7,4)` / pixel top-left `(23,14)`, unextended. Q=`(15,1),r0`, R=`(1,12),r0`, S=`(15,15),r0`.
- Q's ports were directly revealed as `(17,1),(15,2)` logical. R's graph endpoints/ports are `(3,12),(3,14),(3,16)` and S's are `(15,15),(17,16)`; their repeated endpoint convention makes these strongly supported, although they have not separately been selected.
- Computed collision-free tree solution using six extensions costs 25 actions, versus 34 with no growth. Predicted final poses: P `(6,5)` after grow6+left1+down1; Q `(8,1),r2`; R `(4,12),r0`; S `(15,13),r0`. Joins are P-Q `(8,5)`, P-R `(6,12)`, P-S `(15,13)`. All bodies are disjoint.
- Immediate plan is a guarded six-ACTION5 extension probe, with an expectation after every extension. The first mismatch stops the batch. Predicted new branches are left1, right4, left1, right2, left1, right8; after grow6 the root is pixel tile `(26,41)` and long-row endpoint tile `(50,38)`. If confirmed, execute the remaining 19 placement actions; final Q second rotation should raise LEVELS to 5.
- Ruled out: treating ACTION5 on P as rigid rotation; attempting to pair all ten/twelve ports; the predecessor's old 32-action layout that depended on rotating P.