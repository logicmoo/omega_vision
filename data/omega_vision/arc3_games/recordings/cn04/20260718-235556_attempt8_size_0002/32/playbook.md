# Working model

- **Checked through levels 1-5:** Gameplay uses 3x3-pixel logical cells. ACTION1/2/3/4 move the active piece one logical cell up/down/left/right. ACTION5 rotates an ordinary piece 90° clockwise around its unchanged top-left anchor. Pieces may pass through one another; underlying layers are restored when vacated.
- **Checked through levels 1-5:** Color8 endpoint cells are ports. A valid join is exact overlay of two ports and is displayed as color3 while either participant is active. Edge contact, port-on-body, body/body overlap, or three ports at one coordinate are invalid. Completion requires one collision-free assembly pairing every port.
- **Checked through levels 3-5:** Inactive pieces are uniform color4 and hide their ports and piece-specific body color. ACTION6 selects the clicked piece; clicking the active piece can leave none active. Active ports still match hidden inactive ports.
- **Checked on level 5:** A color0 endpoint can be an unfolding root rather than a port. ACTION5 on that rooted piece does not rotate: it consumes the root into body and reveals the next branch row. Level 5 had exactly four deterministic extensions (`RA`, `.AAAAR`, `RA`, `.AAAR`); the fourth removed the root and left a complete seven-port piece. Once unfolded, the ordinary pieces closed all its ports. Do not extrapolate an infinite sequence; later rooted pieces may have different finite rows.
- **Verified level-5 completion:** Exhaustive geometry found one relative 14-port assembly, and executing it raised LEVELS. Thus exposed leaves / merely connecting components is not the objective.
- **Checked:** A full border-hugging strip such as `(16..47,0)` is the deadline HUD, not a piece. It advances roughly every three actions.

# Working memory

- Level 6 of 6, current log step 153, LEVELS=5, background9, fresh 32-cell HUD. There are five tree/path fragments. Active A (color14) occupies logical bbox `(1,1)..(5,6)` and has confirmed ports `(5,3),(3,5),(1,6)`.
- Four inactive components and their graph endpoints: B center, bbox `(6,6)..(8,10)`, endpoints `(8,6),(6,8),(7,10)`; C bottom-left, bbox `(1,12)..(2,17)`, endpoints `(1,12),(2,17)`; D bottom, bbox `(9,14)..(14,18)`, endpoints `(9,17),(11,14),(13,14)`; E top-right, bbox `(15,1)..(18,4)`, endpoints `(15,3),(17,4)`.
- The five graphs have 13 endpoints, but the active piece shows all three of its endpoints as ports. Therefore exactly one inactive endpoint is probably a color0 unfolding root, reducing the initial port count to 12. B is the strong candidate: its bottom endpoint `(7,10)` terminates a vertical trunk with ample space below, matching level 5's rooted-piece visual grammar; its other endpoints should be ports.
- Immediate deliberate probe (also a necessary selection if correct): ACTION6 on B body at pixel `(24,30)`. Predict ports at pixel centers `(27,21)` and `(21,27)`, and a color0 root at `(24,33)`. If confirmed, inspect before using ACTION5; unfold one guarded row at a time until the root disappears, then search all rotations/translations for the shortest collision-free all-port closure.
- Do not yet assume B's extension rows or final port count; level 5 proved those are finite and piece-specific.