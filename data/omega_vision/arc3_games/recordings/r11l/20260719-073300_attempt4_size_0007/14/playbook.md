# Working model
- **Checked:** Control is only `ACTION6(x,y)`. A tethered system has one 5×5 player (color-6 core), N diamond endpoint blobs, and straight color-1 tethers. Its player coordinate is `floor(componentwise mean(endpoint centers))`; this has held for N=2, 3, and 4.
- **Checked:** Exactly one endpoint globally is active, shown by a color-0 body. A normal field click relocates that active endpoint center exactly to the click, with no range limit, then recomputes its system. Clicking any body cell of an inactive color-3 endpoint only selects it (old active 0→3, selected 3→0). Thus changing endpoints normally costs select + destination.
- **Checked:** A player solves a matching dotted ring when its core enters the ring interior; exact center is sufficient but not necessary. Level 3 completed at core `(55,51)`, two cells above ring center `(55,53)`, disproving the old exact-center rule. A solved ring flashes and settles with all 12 perimeter cells changed to 0; its assembly remains until the whole level completes.
- **Checked:** Players and rings can be multicolored. Match the directional color pattern, not one color. On level 4, putting the three-sector `12/14/15` player at the uniquely identical ring `(36,49)` whitened that ring while all seven decoys stayed unchanged.
- **Checked:** Every corresponding player/ring on a multi-player level must be solved; a level advances after the last one.
- **Checked:** Color 2 is wall terrain. The x=0 strip is a fresh 64-action timer per level, filling one cell 0→5 per action; it is not gameplay.
- **Strongly supported by every route, collision itself unprobed:** Treat large connected color-10 regions as forbidden terrain, and require every endpoint/player shape and every exact Bresenham tether to avoid color 2, large color-10 terrain, and other assemblies. Small isolated color-10 cells may instead be dotted-ring parts. `scratch/geom.py` reproduces every observed level-4 tether cell exactly.
- **Checked planning arithmetic:** A final endpoint-coordinate sum in `[N*T, N*T+N-1]` per axis puts the floored mean at exact target T. Obstacles may force waypoint moves or moving a currently inactive endpoint first.

# Working memory
- Level 4, counter 3/6, 5 timer actions used. S15 is confirmed solved at `(36,49)` with endpoints inactive `(36,44)` and active `(36,54)`; its matching `12/14/15` ring is white.
- Remaining S14: endpoints E0=`(17,36)`, E1=`(10,47)`, E2=`(27,52)`, player `(18,45)`, left-14/right-11 matching ring `(50,12)`. Collision-checked shortest route moves each once in order E2→E1→E0: select E2 top `(27,50)`, move `(35,3)` → player `(20,28)`; select E1 top `(10,45)`, move `(58,8)` → `(36,15)`; select E0 top `(17,34)`, move `(57,25)` → exact `(50,12)` and solve. Final endpoint sum `(150,36)`.
- Then remaining S9: current endpoints upper `(46,36)`, lower `(46,52)`, player `(46,44)`, left-9/right-8 target `(17,54)`. With solved S15 and final S14 occupied: select lower top `(46,50)`, move `(14,49)` → player `(30,42)`; select upper top `(46,34)`, move `(20,59)` → exact `(17,54)` and finish level 4. Final sum `(34,108)`.
- All intermediate/full configurations above were checked using exact endpoint/player masks and exact Bresenham tethers against color-2, central size-223 color-10 terrain, and the other assemblies. Minimum static clearance is sqrt(2) for S14 and 2 cells for S9; no occupied cells intersect.
